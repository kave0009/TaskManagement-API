spring:
  application:
    name: Task Management API
  datasource:
    url: jdbc:mysql://registrarmysql.mysql.database.azure.com:3306/TaskManagementDB
    username: Artin
    password: "@Rtin1382"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

vaadin:
  frontend:
    frontend:
      folder: classpath:/static/
  productionMode: true
